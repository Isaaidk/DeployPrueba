# Docker-specific configuration for ReportService

# The database is on the same Docker network, so we use the service name 'votaya-db'
spring.datasource.url=jdbc:postgresql://${DB_HOST:votaya-db}:5432/votayadb